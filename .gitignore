node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Content edited through the admin panel (runtime state, not source).
# Anchored to the repo root so it does not also swallow src/data.
/data/
